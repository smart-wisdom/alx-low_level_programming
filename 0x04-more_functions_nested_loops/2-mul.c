#include "main.h"
/**
 * mul - function that multiplies two integers
 * @a: check the first number to multiply
 * @b: check the second number to multiply by the first number
 * Return: result multiplied by a and b parameter
 */
int mul(int a, int b)
{
	return (a * b);
}
