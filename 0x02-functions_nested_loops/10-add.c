#include "main.h"
#include <stdio.h>
/**
 * add - function that adds two integers and returns the result
 * @n1: check number the of first added integer
 * @n2: check number of the second added integer
 * Return: Value
 */
int add(int n2, int n1)
{
	return (n1 + n2);
}
